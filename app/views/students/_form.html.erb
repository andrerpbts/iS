<%= simple_form_for student do |f| %>
  <%= f.input :name %>
  <%= f.input :register_number %>
  <%= f.input :status,
              collection: Student.status.options,
              include_blank: false %>

  <%= f.submit %>
<% end %>
